# TESSERA Python pipeline.

# Live webcam demo (tessera_cam.py) — vision pipeline that calls the Rust
# solvers (classical SA + real quantum annealing) via ctypes.
# IMPORTANT: must be opencv-CONTRIB (SLIC superpixels live in cv2.ximgproc).
# If you have plain opencv-python installed, uninstall it first:
#   pip uninstall -y opencv-python opencv-contrib-python
#   pip install opencv-contrib-python
opencv-contrib-python>=4.8
numpy>=1.24

# Phase 2 — GNN neural guidance (gnn_guide.py). Pure-torch message passing,
# so torch alone suffices (torch-geometric NOT required). CUDA build recommended.
torch>=2.2
# matplotlib>=3.7   # optional, for plotting
